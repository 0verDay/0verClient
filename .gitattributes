# ============================================================
# 行尾策略 —— 钉死在仓库里，不再依赖各机器的 core.autocrlf
# ============================================================
#
# 背景：GitHub Desktop 自带的 git 把 core.autocrlf=true 设在 **system 级**。
# 没有这个文件的话，"工作区里是什么行尾"就由每台机器的本地配置决定：
# 同一份源码，A 机器检出 LF、B 机器检出 CRLF。
# 更麻烦的是，等到哪天再补规则时，git 会重新归一化全部文件，
# 产生一次"所有文件都改了"的巨型 diff，把真实改动淹没在噪音里。
#
# 本仓库所有源文件本来就是 LF（含 .ps1 / .xaml / .cs），而且这个状态
# 已实测可编译可运行（7 个工程 0 error、43 项自检全过），
# 所以统一钉成 LF：仓库内存 LF，检出也还是 LF，跟现状完全一致。
* text=auto eol=lf

# ------------------------------------------------------------
# 例外：批处理文件用 CRLF
# ------------------------------------------------------------
# cmd.exe 对 LF-only 的 .cmd 在 goto / 标签 / 多行括号块上存在边界问题
# （samples/TestPack/start.cmd 里就留了注释记录这个坑）。
# 批处理是 Windows 专属产物，按原生约定用 CRLF 更省心。
*.cmd text eol=crlf
*.bat text eol=crlf

# ------------------------------------------------------------
# 二进制：禁止任何行尾转换
# ------------------------------------------------------------
# 这些本来就被 .gitignore 挡在外面（dist/ 等）。列在这里是防御性的：
# 万一将来用 git add -f 强加进来，也不会被当成文本改坏。
*.exe  binary
*.dll  binary
*.pdb  binary
*.zip  binary
*.ico  binary
*.png  binary

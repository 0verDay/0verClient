# ============================================================
# 构建产物 —— 绝不提交
# ============================================================
bin/
obj/
artifacts/
.build/
dist/
build/

# 本地工具缓存（正常都在 %USERPROFILE%，列在这里以防被指到仓库内）
.dotnet/
.nuget/
.tmp/

# ============================================================
# IDE
# ============================================================
.vs/
.idea/
*.user
*.suo
*.userosscache
*.sln.docstates

# ============================================================
# 调试遗留
# ============================================================
.probe.txt

# ============================================================
# 本机配置
# ============================================================
# ServerIP.txt 存的是你自己的服务器地址，属于本机配置，不进版本库。
# 新建时照 ServerIP.txt.example 写一行即可。
ServerIP.txt

# ============================================================
# 说明
# ============================================================
# build/ 和 dist/ 都是产物：
#   build/  由 publish-testpack.ps1 / Publish.exe 生成（站点 + 服务器脚本副本）
#   dist/   由 build-dist.ps1 生成（可分发的服务器端与客户端，含内嵌运行时，可到 170 MB）
# 两者都可以只靠源码重新生成，所以不进版本库。
